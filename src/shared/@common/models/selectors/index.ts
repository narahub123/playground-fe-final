import {
  getLanguage,
  getBgTheme,
  getColorTheme,
  getFontSize,
} from "./displaySelectors";

import { getUser, getPassword, getPasswordCheck } from "./userSelectors";

export {
  // display
  getLanguage,
  getBgTheme,
  getColorTheme,
  getFontSize,

  // user
  getUser,
  getPassword,
  getPasswordCheck,
};
