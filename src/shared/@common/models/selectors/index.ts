import {
  getLanguage,
  getBgTheme,
  getColorTheme,
  getFontSize,
} from "./displaySelectors";

import { getPassword, getPasswordCheck } from "./userSelectors";

export {
  // display
  getLanguage,
  getBgTheme,
  getColorTheme,
  getFontSize,

  // user
  getPassword,
  getPasswordCheck,
};
